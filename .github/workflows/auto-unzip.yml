name: Auto Unzip on Uploadon:push:workflow_dispatch:permissions:contents: writejobs:unzip-and-commit:runs-on: ubuntu-lateststeps:- name: Checkout repositoryuses: actions/checkout@v4with:persist-credentials: true- name: Unzip root filerun: |unzip -o "*.zip"
